# Postgres (ex: Neon) — postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=""

# NextAuth — générer avec: openssl rand -base64 32
AUTH_SECRET=""

# Anthropic (chat IA + résumés)
ANTHROPIC_API_KEY=""

# Google Calendar sync — console.cloud.google.com, redirect URI: <app-url>/api/google/callback
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
